"""REST router package.

``main.py`` imports the concrete routers from the submodules:

* ``projects``        -- the ``projects`` registry + dashboard / full-export.
* ``catalogs``        -- the six Class A catalog tables (``routers`` list).
* ``project_tables``  -- the eight other Class B tables (``routers`` list).
"""
