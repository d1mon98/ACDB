"""Pydantic request / response schema package.

Routers import directly from the submodules (``catalogs``, ``projects``,
``dashboard``, ``common``); nothing is re-exported here on purpose so the
origin of each schema stays obvious.
"""
